# HPC Maintenance Tool - Environment Configuration (template)
# Copy this file to ".env" and fill in values. The ".env" file is gitignored.

# OCI tenancy/region
# Prefer OCI_TENANCY_OCID; TENANCY_OCID is also accepted.
OCI_TENANCY_OCID=ocid1.tenancy.oc1..YOUR_TENANCY_OCID_HERE
TENANCY_OCID=
REGION=us-ashburn-1

# Auth
# Uses OCI Instance Principals by default

# Logging
LOG_LEVEL=INFO
LOG_FILE=maint_orchestrator.log
# Optional JSON logging config path used by felix.common.setup_logging
# LOG_CFG_FILEPATH=./logging.json

# Polling intervals (seconds)
DRAIN_POLL_SEC=30
MAINT_POLL_SEC=86400

# Scheduler guardrails
DAILY_SCHEDULE_CAP=10
LOOP_INTERVAL_SEC=900

# Processed tag for maintenance events
PROCESSED_TAG=maintenance_processed

# Slurm integration

# External management CLI (if not on default path)
# MGMT_MANAGE_PATH=/config/mgmt/manage.py

# Event audit log
EVENTS_LOG_FILE=logs/events.jsonl

# Approved fault codes
# If config/approved_fault_codes.json is missing/empty, this env var is used as fallback (comma-separated)
APPROVED_FAULT_CODES=HPCRDMA-0002-02
APPROVED_FAULT_CODES_FILE=config/approved_fault_codes.json

# Excluded hosts (JSON array file)
EXCLUDED_HOSTS_FILE=config/excluded_hosts.json
